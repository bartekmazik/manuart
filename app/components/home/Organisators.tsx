import React from "react";
import { motion } from "motion/react";

const Organisators = () => {
  return (
    <motion.section>
      <div>organisators</div>
    </motion.section>
  );
};

export default Organisators;
